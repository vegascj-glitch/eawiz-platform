import { InterviewTracker } from '@/components/tools/interview-tracker/InterviewTracker';

export const metadata = {
  title: 'Interview Tracker | EAwiz',
  description: 'Track job applications and interview events to stay organized during your job search.',
};

export default function InterviewTrackerPage() {
  return <InterviewTracker />;
}
